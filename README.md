# 🌱 Plant Disease Detector

AI-powered web app to detect plant diseases from leaf images.

Features: Upload leaf image → see disease, symptoms, treatment & prevention. Stores recent scans.

Tech: Node.js, Express, HTML/CSS/JS, Google Gemini AI.

Setup:
1. Clone repo & run `npm install`.
2. Add `.env` with `GEMINI_API_KEY`.
3. Run `node server.js` → open `http://localhost:3000`.

Note: Guidance only, not a replacement for experts.

Authors: Eden Mersha & Meaza Raswork
